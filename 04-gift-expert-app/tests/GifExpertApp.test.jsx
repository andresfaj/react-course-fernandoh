import { render } from "@testing-library/react";
import GiftExpertApp from "../src/GiftExpertApp";

describe("Test on GiftExpertApp Component", () => {
  test("should first", () => {
    render(<GiftExpertApp />);
  });
});
